/* eslint-disable no-console */
const { SHA256 } = require('crypto-js');
const jwt = require('jsonwebtoken');



/*
let message = 'I am number 3!';
let hash = SHA256(message).toString();

console.log(`Message: ${message} hashed to ${hash}`);
*/

